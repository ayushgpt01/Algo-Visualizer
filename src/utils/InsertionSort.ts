import { AnimationItem } from "../Types/VisualizerTypes";
import { swap } from "./MathUtils";

export const InsertionSort = (arr: number[]): AnimationItem[] => {
  const sortedArray = [...arr];
  const anims: AnimationItem[] = []; // {index1,index2,didWeSwap}

  return anims;
};
